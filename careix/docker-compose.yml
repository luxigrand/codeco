services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: careix
      POSTGRES_PASSWORD: careix
      POSTGRES_DB: careix
    ports:
      - "5432:5432"
    volumes:
      - careix_pg:/var/lib/postgresql/data

volumes:
  careix_pg:
